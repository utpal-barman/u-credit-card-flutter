# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See [AGENTS.md](AGENTS.md) for the full project context: what the package is, public API surface, architecture (layout invariants, flip animation, controller bridge, card-network detection, bundled assets), commands, CI gates, and repo conventions. That file is the source of truth — keep guidance there so it stays consistent across all coding agents (Claude Code, Cursor, Aider, etc.). Add Claude-specific overrides below only if they diverge from `AGENTS.md`.
